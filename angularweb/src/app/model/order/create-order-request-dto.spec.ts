import { CreateOrderRequestDto } from './create-order-request-dto';

describe('CreateOrderRequestDto', () => {
  it('should create an instance', () => {
    expect(new CreateOrderRequestDto()).toBeTruthy();
  });
});
