import { CreateBookRequestDto } from './create-book-request-dto';

describe('CreateBookRequestDto', () => {
  it('should create an instance', () => {
    expect(new CreateBookRequestDto()).toBeTruthy();
  });
});
