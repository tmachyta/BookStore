import { UserLoginRequestDto } from './user-login-request-dto';

describe('UserLoginRequestDto', () => {
  it('should create an instance', () => {
    expect(new UserLoginRequestDto()).toBeTruthy();
  });
});
