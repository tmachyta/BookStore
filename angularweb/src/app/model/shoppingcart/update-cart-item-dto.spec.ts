import { UpdateCartItemDto } from './update-cart-item-dto';

describe('UpdateCartItemDto', () => {
  it('should create an instance', () => {
    expect(new UpdateCartItemDto()).toBeTruthy();
  });
});
